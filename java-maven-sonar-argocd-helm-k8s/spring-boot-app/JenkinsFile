pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        dir(java-maven-sonar-argocd-helm-k8s/spring-boot-app) 
        {
        // Run Maven package goal
        sh 'mvn clean package'
        }
    }
    }
  }
}