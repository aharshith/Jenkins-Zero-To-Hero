pipeline {
  agent any
  stages {
    stage(checkout) {
      steps (
        echo 'cloning git repository'
        git branch: 'main', url: 'https://github.com/aharshith/Jenkins-Zero-To-Hero.git
      )
    }
  }
}